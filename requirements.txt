# Core Spark & Delta (Must match Bitnami Spark image 3.5.x)
pyspark==3.5.1
delta-spark==3.1.0

# Telemetry ETL Layer
requests==2.31.0
pandas==2.2.1

# Analytical Database Layer
duckdb==0.10.1

# Utilities
python-dotenv==1.0.1
jupyterlab==4.1.5  # Optional: Helpful for prototyping the ETL logic